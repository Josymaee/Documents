// Josy Ramirez, TEMPLATE

#include <stdio.h>
#include <string.h>
char subject[50];

char name[]= "Voctoria";
char sentence[] = "The quick brown "

int main(void){
       //printf("What class are you in?");
        //scanf("%s", subject);
        //fgets(subject, 50, stdin)
       // printf("You are in %s, that is a cool class", subject)
       //name[0]= 'T';
       //name[1]= 'O';
      // name[2]= 'R';
      // name[3]= 'I';
     //  printf("%s", name);
     //print("%lu\n",sizeof(sentence));
     //printf("%d\n",strlen(sentence));
     printf("%s\n", one);
     char one[]= "Hello ";
     char two[] + "world! ";
     char three[] = "Welcome to my program. ";
     strcat(one, two);
     printf("%s\n", one);
     strcat(three, one);//can only concatenate 2 things at a time
        return 0;
} 